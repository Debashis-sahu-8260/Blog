<!DOCTYPE html>
<html>
<head>
  <style>
    .image-style {
      width: 100%; 
      max-width: 500px; 
      height: auto; 
    }

    .default-avatar {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      background-color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-right: 10px;
    }
  </style>
  <!-- Include CSS stylesheets and meta tags -->
</head>
<body>
  <header>
    <!-- Navigation links -->
  </header>

  <main>
    <div class="container">
      <h1>Welcome to My Blog</h1>
      
      <div class="posts">
        <% @blogings.each do |bloging| %>
          <div class="post">
            <% if bloging.user.avatar? %>
              <%= image_tag bloging.user.avatar.url, class: "avatar" %>
            <% else %>
              <div class="default-avatar"><img src="https://tse3.mm.bing.net/th?id=OIP.Cl56H6WgxJ8npVqyhefTdQHaHa&pid=Api&P=0&h=180" alt="default-avatar" class="default-avatar"></div>
            <% end %>
            <h2><%= link_to bloging.title, bloging_path(bloging) %></h2>
            <p><strong>Email:</strong> <%= bloging.user.email %></p>
            <p><strong>Posted on:</strong> <%= bloging.created_at.strftime('%B %d, %Y') %></p>
            <p><%= truncate(bloging.content, length: 150) %></p>
            <% if bloging.image.present? %>
              <%= image_tag bloging.image.url, class: 'image-style' %>
            <% end %>
            


            <!--ul>
              <% @blogings.each do |bloging| %>
                <li>
                  <strong><%= bloging.title %></strong>
                  <%= link_to "Delete", bloging, data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this Bloging?" } %>
                </li>
              <% end %>
            </ul-->
            <div>
              <%= link_to "Delete", bloging, data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this Bloging?" } %>
            </div>
          </div>
        <% end %>
      </div>

      <div class="create-post">
        <%= link_to 'Create New Post', new_bloging_path %>
      </div>
    </div>
  </main>

  <footer>
    <!-- Footer content -->
  </footer>
</body>
</html>
<%= link_to "logout", destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm:  "are you sure?"}%>